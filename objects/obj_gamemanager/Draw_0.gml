/// @description Insert description here
// You can write your code in this editor

// draw score
draw_set_font(font);
draw_text(80, 8, string_replace_all(string_format(global.score, 8, 0), " ", "0"));

// draw lives
for (var i = 0; i < global.lives; ++i) {
	draw_sprite(spr_pacman, 0, 8+(16*i), 280);
}

// draw win screen
if ((instance_number(obj_pellet) == 0) && (instance_number(obj_pellet) == 0)) {
	draw_sprite(spr_yourewinner, image_index, 0, 0);
}
































































































































































